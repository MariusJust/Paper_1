version_base: 1.1

defaults:
  - _self_
  - config       # pulls in config/config.yaml

# Monte Carlo–specific block
mc:
  reps:         1
  base_seed:    0
  specifications:
    - linear
    - q_Leirvik
    # - interaction

  breakpoints:
    - 2

instance:
  model_selection: 'CV'

hydra:
  hydra_logging:
    level: DISABLED
  job_logging:
    level: WARNING
  verbose: false

